import React from 'react';
import {
  StyleSheet,
  Text,
  View,
} from 'react-native';

function BasicText({ text }) {
  return (
    <View>
      <Text>{text}</Text>
    </View>
  );
}

export default BasicText;
